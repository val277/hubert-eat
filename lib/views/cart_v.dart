import 'package:flutter/material.dart';

class CartV extends StatelessWidget {
  const CartV({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Cart"));
  }
}
