import 'package:flutter/material.dart';

class OrdersV extends StatelessWidget {
  const OrdersV({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Orders"));
  }
}
